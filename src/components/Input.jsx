import React from 'react';

const Input = (props) => {
    return (
        <input type={props.type}  id="Frame1328" placeholder={props.placeholder} name={props.name} />
    );
};

export default Input;